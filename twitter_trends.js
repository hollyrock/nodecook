// Working with real data -fetting trending tweets

var https = request('https');
var output = require('fs').createWriteStream('auth.json');
var consumer = {
		key: '<our consumer key>',
		secret: '<our consumer secret>'>'
		};

var request = https.request({
		method: 'POST',
		host: 'api.

